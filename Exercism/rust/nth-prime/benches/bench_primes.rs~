#[macro_use]
extern crate criterion;

use criterion::Criterion;
use nth_prime as np;

fn criterion_benchmark(c: &mut Criterion) {
    c.bench_function("nth-prime 30", |b| b.iter(|| np.nth(30)));
}

criterion_group!(benches, criterion_benchmark);
criterion_main!(benches);
